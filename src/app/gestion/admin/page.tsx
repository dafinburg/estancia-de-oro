import GestionShell from '@/components/GestionShell';
import PanelUploadMasivo from '@/components/PanelUploadMasivo';

export default function AdminPage() {
  return (
    <GestionShell>
      <PanelUploadMasivo />
    </GestionShell>
  );
}
