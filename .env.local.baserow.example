# Credenciales para correr scripts/baserow/setup.mjs LOCALMENTE.
# No se sube a git (ver .gitignore). Baserow necesita JWT (user/password) para
# crear tablas — los API Tokens solo sirven para filas.

BASEROW_URL=https://baserow.mtrpymes.com.ar
BASEROW_USER=diegofainburg@mtrpymes.com.ar
BASEROW_PASSWORD=tu_password
BASEROW_DATABASE_ID=213
